import java.util.Collections;importjava.util.*;
public class Collection_iterators
        {
   public static void main(Stringa args[])
   {
         LinkedListlist=newLinkedList();
         list.add(newInteger(2));
         list.add(newInteger(8));
         list.add(newInteger(5));
         list.add(newInteger(1));
         Iteratori=list.iterator();
         Collections.reverse(list);
         Collections.shuffle(list);
         i.next();
         i.remove();
         while(i.hasNext())
         System.out.print(i.next()+"");
         }
         }